OBJECT Page 7373 Bin Creation Wksh. Name List
{
  OBJECT-PROPERTIES
  {
    Date=;
    Time=;
    Version List=NAVW114.00;
  }
  PROPERTIES
  {
    Editable=No;
    CaptionML=[ENU=Bin Creation Wksh. Name List;
               NOR=Navn pÜ hylleoppr.frsl - ovrs.;
               SVE=Lagerpl.upplÑgg fîrslagnamnl.];
    SourceTable=Table7337;
    DataCaptionExpr=DataCaption;
    DelayedInsert=Yes;
    PageType=List;
    OnOpenPage=BEGIN
                 BinCreateLine.OpenWkshBatch(Rec);
               END;

    OnFindRecord=BEGIN
                   IF FIND(Which) THEN BEGIN
                     BinCreateName := Rec;
                     WHILE TRUE DO BEGIN
                       IF WMSManagement.LocationIsAllowed("Location Code") THEN
                         EXIT(TRUE);
                       IF NEXT(1) = 0 THEN BEGIN
                         Rec := BinCreateName;
                         IF FIND(Which) THEN
                           WHILE TRUE DO BEGIN
                             IF WMSManagement.LocationIsAllowed("Location Code") THEN
                               EXIT(TRUE);
                             IF NEXT(-1) = 0 THEN
                               EXIT(FALSE);
                           END;
                       END;
                     END;
                   END;
                   EXIT(FALSE);
                 END;

    OnNextRecord=VAR
                   RealSteps@1001 : Integer;
                   NextSteps@1002 : Integer;
                 BEGIN
                   IF Steps = 0 THEN
                     EXIT;

                   BinCreateName := Rec;
                   REPEAT
                     NextSteps := NEXT(Steps / ABS(Steps));
                     IF WMSManagement.LocationIsAllowed("Location Code") THEN BEGIN
                       RealSteps := RealSteps + NextSteps;
                       BinCreateName := Rec;
                     END;
                   UNTIL (NextSteps = 0) OR (RealSteps = Steps);
                   Rec := BinCreateName;
                   FIND;
                   EXIT(RealSteps);
                 END;

    ActionList=ACTIONS
    {
      { 1900000004;0 ;ActionContainer;
                      ActionContainerType=ActionItems }
      { 6       ;1   ;Action    ;
                      ShortCutKey=Return;
                      CaptionML=[ENU=Edit Worksheet;
                                 NOR=Rediger forslag;
                                 SVE=Redigera fîrslag];
                      ToolTipML=[ENU=Open the related worksheet.;
                                 NOR=èpne det tilknyttede forslaget.;
                                 SVE=ôppna det relaterade fîrslaget.];
                      ApplicationArea=#Warehouse;
                      Promoted=Yes;
                      PromotedIsBig=Yes;
                      Image=OpenWorksheet;
                      PromotedCategory=Process;
                      OnAction=BEGIN
                                 BinCreateLine.TemplateSelectionFromBatch(Rec);
                               END;
                                }
    }
  }
  CONTROLS
  {
    { 1900000001;0;Container;
                ContainerType=ContentArea }

    { 1   ;1   ;Group     ;
                GroupType=Repeater }

    { 2   ;2   ;Field     ;
                ToolTipML=[ENU=Specifies a name for the worksheet.;
                           NOR=Angir et navn for forslaget.;
                           SVE=Anger ett namn pÜ fîrslaget.];
                ApplicationArea=#Warehouse;
                SourceExpr=Name }

    { 4   ;2   ;Field     ;
                ToolTipML=[ENU=Specifies a description for the worksheet.;
                           NOR=Angir en beskrivelse for forslaget.;
                           SVE=Anger en beskrivning av fîrslaget.];
                ApplicationArea=#Warehouse;
                SourceExpr=Description }

    { 19  ;2   ;Field     ;
                ToolTipML=[ENU=Specifies the location code for which the worksheet should be used.;
                           NOR=Angir lokasjonskoden som forslaget skal brukes for.;
                           SVE=Anger lagerstÑllekoden fîr vilken fîrslaget bîr anvÑndas.];
                ApplicationArea=#Location;
                SourceExpr="Location Code" }

    { 1900000007;0;Container;
                ContainerType=FactBoxArea }

    { 1900383207;1;Part   ;
                ApplicationArea=#RecordLinks;
                Visible=FALSE;
                PartType=System;
                SystemPartID=RecordLinks }

    { 1905767507;1;Part   ;
                ApplicationArea=#Notes;
                Visible=FALSE;
                PartType=System;
                SystemPartID=Notes }

  }
  CODE
  {
    VAR
      BinCreateLine@1002 : Record 7338;
      BinCreateName@1000 : Record 7337;
      WMSManagement@1001 : Codeunit 7302;

    LOCAL PROCEDURE DataCaption@1() : Text[250];
    VAR
      BinCreateTemplate@1000 : Record 7336;
    BEGIN
      IF NOT CurrPage.LOOKUPMODE THEN
        IF GETFILTER("Worksheet Template Name") <> '' THEN
          IF GETRANGEMIN("Worksheet Template Name") = GETRANGEMAX("Worksheet Template Name") THEN
            IF BinCreateTemplate.GET(GETRANGEMIN("Worksheet Template Name")) THEN
              EXIT(BinCreateTemplate.Name + ' ' + BinCreateTemplate.Description);
    END;

    BEGIN
    END.
  }
}

